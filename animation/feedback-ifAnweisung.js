<script>

if (answer == 2) {
    g.setColor(Color.GREEN);
} else {
    g.setColor(Color.RED);
}
}

if (answer == 3) {
    g.setColor(Color.GREEN);
} else {
    g.setColor(Color.RED)
}
}

if (answer == 2) {
    g.setColor(Color.GREEN);
} else {
    g.setColor(Color.RED);
}
}

if (answer == 1) {
    g.setColor(Color.GREEN);
} else {
    g.setColor(Color.RED);
}
}

if (answer == 1) {
    g.setColor(Color.GREEN);
} else {
    g.setColor(Color.RED);
}
}

if (answer == 2) {
    g.setColor(Color.GREEN);
} else {
    g.setColor(Color.RED);
}
}

if (answer == 2) {
    g.setColor(Color.GREEN);
} else {
    g.setColor(Color.RED);
}
}


if (answer == 3) {
    g.setColor(Color.GREEN);
} else {
    g.setColor(Color.RED);
}
}

if (answer == 2) {
    g.setColor(Color.GREEN);
} else {
    g.setColor(Color.RED);
}
}


if (answer == 2) {
    g.setColor(Color.GREEN);
} else {
    g.setColor(Color.RED);
}
}

</script>